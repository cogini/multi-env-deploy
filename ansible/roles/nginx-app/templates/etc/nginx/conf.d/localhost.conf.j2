server {
    listen        80;
    server_name   127.0.0.1;
    access_log    off;

    location /nginx_status {
        stub_status on;
        allow       127.0.0.1;
        deny        all;
    }
}
